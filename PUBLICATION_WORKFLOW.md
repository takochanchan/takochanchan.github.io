# 公開手順

## 完了条件

新規公開・改訂公開は、公開版の作業正本が非公開リポジトリに保存され、
GitHub上のコミットSHAが公開側の台帳に記録された後にのみ完了とします。

## 手順

1. 図版・地図・複雑な表・ページ依存の組版を含む資料はWord、本文中心の資料は
   Markdownで正本を確定する。HTMLは正本にしない。
2. 正本、`BIBLIOGRAPHY.json`、必要な素材を
   `takochanchan/-archive-masters` の `publications/<slug>/` に保存する。
3. 正本リポジトリの検証、SHA-256照合、DOCXまたはMarkdown検証、
   Git LFS検証を通す。
4. GitHub上で保存後の40桁コミットSHAを確認する。
5. 本リポジトリの `master-archive.json` に、そのSHAと正本パスを記録する。
6. `npm run build` と `npm test` を実行する。
7. 以上が成功した後に、PDF・EPUBをReleaseへ反映し、サイトを公開する。

正本保存、リモート確認、台帳検証のいずれかに失敗した場合は、公開処理を停止します。

## 台帳

`master-archive.json` の `archive_commit` は、全掲載資料を含む正本リポジトリの
最新確認済みコミットです。`publications` は公開資料のslugと正本パスを一対一で対応させます。
資料追加時に台帳を更新しなければ、ビルドとテストは失敗します。

## 公開済み資料の改訂

公開済み資料を訂正・改訂する場合も、新規公開と同じ正本先行ゲートを適用します。
既存URLを維持できる訂正では、slugと公開PDF・EPUBのファイル名を変更しません。

1. 対象slug、現行の正本コミット、公開PDF・EPUB・`SHA256SUMS.txt`の
   容量とSHA-256を記録し、改訂対象を固定する。
2. 編集用原稿と生成スクリプトを修正し、DOCX・PDF・EPUB・Markdownを
   同じ原稿から再生成する。PDFやEPUBだけを個別に直接修正しない。既存版が
   専用Fontconfigや同梱フォントに依存する場合は同じ設定を明示して使い、
   期待頁数から変わった時点で差替えを停止する。
3. 旧表記が原稿、DOCX、PDF抽出テキスト、EPUB本文、書誌、サイト説明に
   残っていないことを検索し、DOCXとPDFは全頁をレンダリングして確認する。
   EPUBはZIP検査だけでなく、EPUB 3、表紙宣言、目次・spine・画像参照・代替
   テキストを`npm run test:epub`相当の正式検査で確認する。
4. 改訂版DOCXまたはMarkdown、更新した`BIBLIOGRAPHY.json`を、公開物より先に
   非公開正本リポジトリへ保存する。公開EPUBの容量とSHA-256も書誌へ反映する。
5. 非公開正本リポジトリの検証成功後、GitHub上の新しい40桁コミットSHAを確認し、
   公開側の`master-archive.json`を更新する。
6. 公開リポジトリでは対象資料のメタデータ、`assets-manifest.json`、
   回帰テストを更新し、`npm run build`と`npm test`を実行する。
7. Releaseの`SHA256SUMS.txt`はアップロード直前に現行版を再取得し、
   対象PDF・EPUBの2行だけを新しい値へ置換する。他資料の行は変更しない。
8. 一時ワークフローは、対象ファイルの容量とSHA-256を照合してから
   `--clobber`で同名資産を差し替え、完了後に一時ブランチと自身を削除する。
9. 書込み直前にリモート`main`を再取得する。基点が変わっていた場合は停止し、
   最新`main`へ対象差分だけを載せ直してから続行する。
10. 公開後、実URLから書誌ページ、PDF、EPUB、`SHA256SUMS.txt`を再取得し、
    表記、頁数、EPUB構造、容量、SHA-256、配信URLを照合する。

改訂作業は、正本更新、公開資産差替え、サイト更新、公開後照合のすべてが
完了した時点で完了とします。
