# 公開手順

## 完了条件

新規公開・改訂公開は、公開版の作業正本が非公開リポジトリに保存され、
GitHub上のコミットSHAが公開側の台帳に記録された後にのみ完了とします。

## 手順

1. 図版・地図・複雑な表・ページ依存の組版を含む資料はWord、本文中心の資料は
   Markdownで正本を確定する。HTMLは正本にしない。
2. 正本、`BIBLIOGRAPHY.json`、必要な素材を
   `takochanchan/-archive-masters` の `publications/<slug>/` に保存する。
3. 正本リポジトリの検証、SHA-256照合、DOCXまたはMarkdown検証、
   Git LFS検証を通す。
4. GitHub上で保存後の40桁コミットSHAを確認する。
5. 本リポジトリの `master-archive.json` に、そのSHAと正本パスを記録する。
6. `npm run build` と `npm test` を実行する。
7. PDF・EPUBをReleaseへ反映し、容量・SHA-256を`assets-manifest.json`へ記録する。
8. `search-shards.json` と各資料の `searchShard` から更新対象の外部索引を確定する。
   既存資料は既定で `001` に属し、同索引は300作品を上限とする。上限到達後の新規資料は
   次の三桁IDを明示し、既存slugを並べ替えだけで別索引へ移さない。
9. 確定した正本（公開ビルドで正本を直接読めない場合は、SHA-256を固定した最終EPUB）と、
   Releaseへ反映したものと同一のPDFから、対象のPagefind索引だけを再生成する。
   各資料の検索結果には正本の底本位置標識とPDF物理頁を持たせ、検索結果URLは必ず
   `publications/<slug>/` の書誌ページとする。同一PDF頁の複数一致は1スニペットにまとめる。
10. 索引リポジトリで `SEARCH_SHARD=<三桁ID> npm run verify:search` を実行し、
    当該索引に属する全資料の収録、正本コミット、PDFのSHA-256、底本位置標識、
    PDF頁対応、書誌ページURLを検査してから、そのProject Pagesへ索引を配備する。
11. 公開用リポジトリで `npm run verify:remote-search` を実行し、外部索引群が現行の
    全slugを重複なく網羅し、正本・資産台帳と一致し、容量上限内にあることを検査する。
12. 以上が成功した後に、本体Pagesを公開する。本体のPages artifactには書誌・個別ページ・
    表紙・検索UIだけを含め、Pagefind索引は含めない。

正本保存、リモート確認、台帳検証、外部検索索引生成、PDF頁対応のいずれかに失敗した場合は、
本体Pagesの配備前に公開処理を停止します。外部索引が現行書誌より古い間はPagesワークフローを
失敗させ、検索と書誌が揃っている直前の公開版を維持します。

## 公開書誌の記述方針

書誌の説明文は資料の内容・主題と主要底本に限定し、形態欄は巻数・PDF頁数などの
書誌的事項だけを記載する。原刊の前付・本文・索引・欄外注・段落・頁順を保持した
という説明、原刊位置標識の件数、母版・フォント・組版、レビュー・監査などの
制作工程・品質管理情報は、公開書誌へ記載しない。これらは必要に応じて私設正本の
`translation_scope` または `working_master` にのみ記録する。

新規・改訂書誌は、私設正本の
`working_master.public_bibliography_policy: "content_source_only_v1"`
と書誌文面検査を通過したものだけを公開側へ反映する。

## Git LFSを利用できない場合

当月のGit LFS帯域またはストレージ枠を使い切り、新規の正本をLFSへ保存できない場合に限り、
非公開正本リポジトリの版別Releaseを一時的な正本保存先として使用します。公開物を先に出して
後から正本を置くことは認めません。

1. slugと改訂日を含む新しい版別タグを作る。既存タグと既存Releaseは上書きしない。
2. 正本、`BIBLIOGRAPHY.json`、権利情報、必要素材、`SHA256SUMS.txt`を同じReleaseへ収める。
3. 各資産のファイル名、容量、SHA-256を照合し、Releaseから再取得したバイトも照合する。
4. 非公開正本リポジトリの`MASTER_RELEASE_LEDGER.json`へ、Releaseタグ・URL・全資産の
   容量とSHA-256・正規のLFS移行先・理由を記録し、状態を`pending_lfs`とする。
5. 台帳コミットがGitHub上の`main`に存在することを確認してから、公開側の
   `master-archive.json`を更新する。公開側では`release_fallbacks`に同じ版別Releaseと
   台帳コミットを記録する。
6. LFSが再び利用可能になったら、Releaseで検証済みの同一バイトを正規パスへ移し、
   通常のLFS検証を行う。移行後も版別Releaseと`MASTER_RELEASE_LEDGER.json`の記録は
   削除・上書きしない。

## 底本位置標識

底本位置標識は本文から独立した段落とし、右寄せで統一します。位置を示す段落と、
本文異同・判読・補筆理由などを説明する訳注は分離します。

- 刊本は `〔原刊 p. 123〕` とする。
- 写本・自筆稿は葉丁の表裏を `f. 12r` / `f. 12v` で示す。資料名や巻の識別が
  必要な場合は `〔自筆稿 第2巻 f. 147r〕` のように前置する。
- 葉丁がなく画像でしか位置を特定できない場合は `〔底本画像 123〕` とする。
  葉丁が確認できる箇所では画像番号を主標識にしない。
- 別本を照合しただけなら、位置標識には主底本だけを置き、別本は訳注に記す。
- 欠落を別本で補った場合は
  `〔主底本 原刊 p. 93／補完底本 写本 f. 46r〕` のように役割を明記する。
- 二本以上を実際に合成した場合は
  `〔合成底本 A：原刊 p. 93／B：写本 f. 46r〕` のように各底本を明示する。
- 本文途中で底本が切り替わる場合は、切替位置に別々の標識段落を置く。

`出所`だけの表記、`fol.` / `fols.`、画像コマと葉丁と説明文を一段落へ混在させる
表記は、新規・改訂正本では用いません。既存資料を改訂するときは意味を失わないよう、
主底本・補完底本・合成底本・照合資料のいずれかへ分類してから移行します。

## 台帳

`master-archive.json` の `archive_commit` は、全掲載資料を含む正本リポジトリの
最新確認済みコミットです。`publications` は公開資料のslugと正本パスを一対一で対応させます。
LFS代替保存中の資料は`release_fallbacks`に正規パス、版別Release、台帳コミット、正本の
容量とSHA-256を記録します。資料追加時にいずれかの台帳を更新しなければ、ビルドとテストは
失敗します。

## 公開済み資料の改訂

公開済み資料を訂正・改訂する場合も、新規公開と同じ正本先行ゲートを適用します。
既存URLを維持できる訂正では、slugと公開PDF・EPUBのファイル名を変更しません。

1. 対象slug、現行の正本コミット、公開PDF・EPUB・`SHA256SUMS.txt`の
   容量とSHA-256を記録し、改訂対象を固定する。
2. 編集用原稿と生成スクリプトを修正し、DOCX・PDF・EPUB・Markdownを
   同じ原稿から再生成する。PDFやEPUBだけを個別に直接修正しない。既存版が
   専用Fontconfigや同梱フォントに依存する場合は同じ設定を明示して使い、
   期待頁数から変わった時点で差替えを停止する。
3. 旧表記が原稿、DOCX、PDF抽出テキスト、EPUB本文、書誌、サイト説明に
   残っていないことを検索し、DOCXとPDFは全頁をレンダリングして確認する。
   EPUBはZIP検査だけでなく、EPUB 3、表紙宣言、目次・spine・画像参照・代替
   テキストを`npm run test:epub`相当の正式検査で確認する。
4. 改訂版DOCXまたはMarkdown、更新した`BIBLIOGRAPHY.json`を、公開物より先に
   非公開正本リポジトリへ保存する。公開EPUBの容量とSHA-256も書誌へ反映する。
5. 非公開正本リポジトリの検証成功後、GitHub上の新しい40桁コミットSHAを確認し、
   公開側の`master-archive.json`を更新する。
6. 公開リポジトリでは対象資料のメタデータ、`assets-manifest.json`、
   回帰テストを更新し、`npm run build`と`npm test`を実行する。
7. Releaseの`SHA256SUMS.txt`はアップロード直前に現行版を再取得し、
   対象PDF・EPUBの2行だけを新しい値へ置換する。他資料の行は変更しない。
8. 一時ワークフローは、対象ファイルの容量とSHA-256を照合してから
   `--clobber`で同名資産を差し替え、完了後に一時ブランチと自身を削除する。
9. 差替え後の最終PDFと正本から対象の外部全文検索索引を再生成し、
   `SEARCH_SHARD=<三桁ID> npm run verify:search`で
   対象slug、正本コミット、PDF SHA-256、底本位置標識、PDF物理頁、書誌URLを照合する。
10. 書込み直前にリモート`main`を再取得する。基点が変わっていた場合は停止し、
   最新`main`へ対象差分だけを載せ直してから続行する。
11. 外部索引の配備後に `npm run verify:remote-search` を通し、その後で本体Pagesを配備する。
12. 公開後、実URLから書誌ページ、PDF、EPUB、`SHA256SUMS.txt`、外部検索索引を再取得し、
    表記、頁数、EPUB構造、容量、SHA-256、配信URL、検索結果の底本位置標識・PDF頁を照合する。

改訂作業は、正本更新、公開資産差替え、外部検索索引更新、サイト更新、公開後照合のすべてが
完了した時点で完了とします。
