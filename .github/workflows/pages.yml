name: Upload Squier batch 03 release assets

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: squier-batch03-release-assets
  cancel-in-progress: false

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Check out staged release data
        uses: actions/checkout@v4
        with:
          ref: agent/squier-batch03-release-assets-20260826-v1
          path: staged

      - name: Reconstruct and verify release files
        run: |
          node --input-type=module <<'NODE'
          import { createHash } from "node:crypto";
          import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
          import { basename, join } from "node:path";
          const root = "staged";
          const manifest = JSON.parse(readFileSync(join(root, "manifest.json"), "utf8"));
          mkdirSync("release-assets", { recursive: true });
          for (const asset of manifest.assets) {
            if (basename(asset.name) !== asset.name) throw new Error(`Invalid asset name: ${asset.name}`);
            const bytes = Buffer.concat(asset.chunks.map((part) => readFileSync(join(root, part))));
            const digest = createHash("sha256").update(bytes).digest("hex");
            if (bytes.length !== asset.size || digest !== asset.sha256) {
              throw new Error(`Checksum mismatch: ${asset.name}`);
            }
            writeFileSync(join("release-assets", asset.name), bytes);
          }
          NODE

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload publications-current release-assets/* --clobber --repo "$GITHUB_REPOSITORY"

      - name: Download and verify uploaded assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir verified
          for asset in release-assets/*; do
            gh release download publications-current --pattern "$(basename "$asset")" --dir verified --repo "$GITHUB_REPOSITORY"
          done
          node --input-type=module <<'NODE'
          import { createHash } from "node:crypto";
          import { readFileSync } from "node:fs";
          import { join } from "node:path";
          const manifest = JSON.parse(readFileSync("staged/manifest.json", "utf8"));
          for (const asset of manifest.assets) {
            const bytes = readFileSync(join("verified", asset.name));
            const digest = createHash("sha256").update(bytes).digest("hex");
            if (bytes.length !== asset.size || digest !== asset.sha256) {
              throw new Error(`Downloaded asset mismatch: ${asset.name}`);
            }
          }
          NODE
