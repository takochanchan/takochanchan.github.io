name: Publish Ixtlilxochitl five-work release assets

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/publish-ixtlilxochitl-five-works-assets-20260812.yml

permissions:
  contents: write

concurrency:
  group: publications-current-release
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      WORKFLOW_PATH: .github/workflows/publish-ixtlilxochitl-five-works-assets-20260812.yml
      STAGING_BRANCH: agent/ixtlilxochitl-five-works-assets-20260812
      ASSET_MANIFEST: |-
          [
            {
              "slug": "ixtlilxochitl-compendio-tetzcoco",
              "kind": "epub",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Compendio_Historico_Reyes_Tetzcoco_Japanese_Complete_Translation.epub",
              "title": "テツココ諸王の歴史綱要",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 2,
              "folioCount": 135,
              "size": 888214,
              "sha256": "4512e281a16896c9f0b53f5e22f7e7bbd19331d1d84827123a3ab5577ab3ac64",
              "blobs": [
                "84ee6b565d67244f6ada709e476ceca9338d9c2e",
                "3fea223ef30be5f879081e2f047642ccdca6e52b"
              ],
              "minJapanese": 100000,
              "imageRefs": 1
            },
            {
              "slug": "ixtlilxochitl-compendio-tetzcoco",
              "kind": "pdf",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Compendio_Historico_Reyes_Tetzcoco_Japanese_Complete_Translation.pdf",
              "title": "テツココ諸王の歴史綱要",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 2,
              "folioCount": 135,
              "size": 2273490,
              "sha256": "6572d91df0f2d1ed3cbdc9eacee59b0e6e7913db5487d7d340d9030108bd14da",
              "blobs": [
                "9caf8f21407b09333efb75ec7edc7a1bf157d91d",
                "f0008f95ce63f91f683c1667fefa4961bef1c90f",
                "c5179950c08f96fe86bea8845d221bdd1777e572",
                "cda93d3db31c4e7cd2016f5a3879b4b9176a4f64",
                "e78fcd1e6ed009214cc9360e66df46c10d77a3c9"
              ],
              "pages": 147,
              "imageCount": 2
            },
            {
              "slug": "ixtlilxochitl-historia-chichimeca",
              "kind": "epub",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Historia_Nacion_Chichimeca_Japanese_Complete_Translation.epub",
              "title": "チチメカ民族史",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 2,
              "folioCount": 286,
              "size": 1611418,
              "sha256": "3495f7db63b3c6674991ff60c35174a397cc96f450a8a14865fa3a05fad667a4",
              "blobs": [
                "28747c169abae97d56bf3c9a82ff89ec0923a7eb",
                "2be6b7d0c73857ebb205c9444583272927197e56",
                "c2d699b25910a20b392122ffecb29b4fbf05d785",
                "b9871ef879f43986abc88c72c604e22f19a3c22a"
              ],
              "minJapanese": 200000,
              "imageRefs": 1
            },
            {
              "slug": "ixtlilxochitl-historia-chichimeca",
              "kind": "pdf",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Historia_Nacion_Chichimeca_Japanese_Complete_Translation.pdf",
              "title": "チチメカ民族史",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 2,
              "folioCount": 286,
              "size": 3786480,
              "sha256": "8ce157a8b9d9bc1b964304dc5af8efbd41850fc41fe8e292cbdd1769e9ae22f8",
              "blobs": [
                "282debb547f55477ade2af8e66b9f4b84c1af226",
                "098f467e5992d04d96a9d2c7a58d299088b217aa",
                "a4cf8d1e26708a40076121203094d2d049e3b88c",
                "171b31cb96d5da70f2e8f0fb85932af85437f1b3",
                "e09add7c40019c7f561c54b8eba2d3acf098e8c3",
                "eeef8db809dc859c4e4ee22c93b6b17e6dd4dd76",
                "f88d2f9de040186421a6932865c42e4b64a26b6d",
                "7337b5eb790de7708ac5c5653bf8e507854e9900"
              ],
              "pages": 319,
              "imageCount": 2,
              "tailOrder": [
                "f. 140v",
                "f. 142v",
                "f. 142r",
                "f. 141v",
                "f. 141r",
                "f. 143r",
                "f. 143v"
              ]
            },
            {
              "slug": "ixtlilxochitl-relacion-sucinta",
              "kind": "epub",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Relacion_Sucinta_Memorial_Japanese_Complete_Translation.epub",
              "title": "ヌエバ・エスパーニャ史簡略報告",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 1,
              "folioCount": 18,
              "size": 1371504,
              "sha256": "6e964987d6118e4d41664131776a61fdbba44b5b55b75b833c3f7c2f0753b4c0",
              "blobs": [
                "74944a342037b9defd4a3d53c324381a36c84b2f",
                "0e0e617abfffb2c60c78165f23cf2a5d3b0943e6",
                "0e343fd8dcae043f28ebb5d944740f7b8bf4018b"
              ],
              "minJapanese": 17000,
              "imageRefs": 1
            },
            {
              "slug": "ixtlilxochitl-relacion-sucinta",
              "kind": "pdf",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Relacion_Sucinta_Memorial_Japanese_Complete_Translation.pdf",
              "title": "ヌエバ・エスパーニャ史簡略報告",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 1,
              "folioCount": 18,
              "size": 1403090,
              "sha256": "d41ef302a9c9154a841e20cba6b97f6ccdf17c6b889de036a2d427678e659e8f",
              "blobs": [
                "c678a2afbc8d27b27291362baf982f88b308dbff",
                "b7ccfbf4213c9fd501047438e965101b2bf10ed0",
                "7f488718ff8d07ee449a99b226be6b147a0bde7e"
              ],
              "pages": 34,
              "imageCount": 2
            },
            {
              "slug": "ixtlilxochitl-sumaria-general",
              "kind": "epub",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Sumaria_Relacion_Historia_General_Japanese_Complete_Translation.epub",
              "title": "ヌエバ・エスパーニャ通史略報",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 1,
              "folioCount": 30,
              "size": 633307,
              "sha256": "cadafdefae9e5a0ae4da50e19ecd7dc500ce131d22af8213aa5b78910922cdbf",
              "blobs": [
                "9c47d82980ab6612600cf7e79fd4ee7199626cdf",
                "bb9c7c9972fe5036cc9c26827ea80adf2f3b6de9"
              ],
              "minJapanese": 20000,
              "imageRefs": 1
            },
            {
              "slug": "ixtlilxochitl-sumaria-general",
              "kind": "pdf",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Sumaria_Relacion_Historia_General_Japanese_Complete_Translation.pdf",
              "title": "ヌエバ・エスパーニャ通史略報",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 1,
              "folioCount": 30,
              "size": 782212,
              "sha256": "b346eca15c925b72af8502573e6590efc62e13e753788fdc323fff3a4567d455",
              "blobs": [
                "9f247d2cdfabd4f9de6f00ed018ffd7e6cf093d6",
                "745ed040583d724efad54526ee05a75d01327fef"
              ],
              "pages": 29,
              "imageCount": 2
            },
            {
              "slug": "ixtlilxochitl-sumaria-todas",
              "kind": "epub",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Sumaria_Relacion_Todas_Cosas_Japanese_Complete_Translation.epub",
              "title": "ヌエバ・エスパーニャ諸事略報",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 1,
              "folioCount": 162,
              "size": 719992,
              "sha256": "986823ec606ddc39478ff91105c29ee270dfd8768becfa10628012c0837e4b2c",
              "blobs": [
                "42e3cd9ac6b9774a6e3394fc4919937ab836d8de",
                "ff0af4f8831a6556e4339bfc2e765af4404d1123"
              ],
              "minJapanese": 120000,
              "imageRefs": 1
            },
            {
              "slug": "ixtlilxochitl-sumaria-todas",
              "kind": "pdf",
              "name": "Fernando_de_Alva_Ixtlilxochitl_Sumaria_Relacion_Todas_Cosas_Japanese_Complete_Translation.pdf",
              "title": "ヌエバ・エスパーニャ諸事略報",
              "author": "フェルナンド・デ・アルバ・イシュトリルショチトル",
              "volume": 1,
              "folioCount": 162,
              "size": 2410649,
              "sha256": "33a0b0b1054a618ab53817d12a488957d579194dd6b9f4a4e327bc0c46962171",
              "blobs": [
                "54534b3d2013b27481a7c7bb5ebc0c740c637347",
                "2c85b714fe83d47fcef152ff881a6a0a1ed9faaa",
                "cf4fe44e1b233ba7c126af93cfc4416b3eda014b",
                "e07f5d87ac987eeb219cd77b9a0cb660043bb7d8",
                "f429857358075aa7771cf9766d706dd5398c070f"
              ],
              "pages": 179,
              "imageCount": 2
            }
          ]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install PDF inspection tools
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils

      - name: Reassemble and validate release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/ixtlilxochitl-assets
          python3 - <<'PY'
          import base64
          import hashlib
          import json
          import os
          import re
          import subprocess
          import zipfile
          from pathlib import Path
          from xml.etree import ElementTree as ET

          assets = json.loads(os.environ["ASSET_MANIFEST"])
          root = Path("/tmp/ixtlilxochitl-assets")
          repository = os.environ["GITHUB_REPOSITORY"]
          forbidden = (
              "GPT",
              "ChatGPT",
              "確認用",
              "Proof",
              "未公開",
              "公開承認前",
              "翻訳作業稿",
              "本日本語版",
              "日本語翻訳版",
              "再利用ライセンス",
          )
          xhtml_ns = "http://www.w3.org/1999/xhtml"
          opf_ns = "http://www.idpf.org/2007/opf"

          for asset in assets:
              target = root / asset["name"]
              with target.open("wb") as output:
                  for blob_sha in asset["blobs"]:
                      encoded = subprocess.check_output(
                          [
                              "gh",
                              "api",
                              f"repos/{repository}/git/blobs/{blob_sha}",
                              "--jq",
                              ".content",
                          ],
                          text=True,
                      )
                      output.write(base64.b64decode("".join(encoded.split())))

              data = target.read_bytes()
              assert len(data) == asset["size"], (asset["name"], len(data))
              assert hashlib.sha256(data).hexdigest() == asset["sha256"]

              marker = re.compile(
                  rf"〔自筆稿\s+第\s*{asset['volume']}\s*巻\s+f\.\s*[^〕]+〕"
              )
              if asset["kind"] == "pdf":
                  info = subprocess.check_output(["pdfinfo", str(target)], text=True)
                  pages = int(re.search(r"^Pages:\s+(\d+)$", info, re.M).group(1))
                  assert pages == asset["pages"], (asset["name"], pages)
                  assert "612 x 792 pts (letter)" in info
                  assert re.search(r"^Tagged:\s+yes$", info, re.M)
                  assert re.search(r"^JavaScript:\s+no$", info, re.M)
                  assert re.search(r"^Encrypted:\s+no$", info, re.M)

                  fonts = subprocess.check_output(["pdffonts", str(target)], text=True)
                  font_lines = [line for line in fonts.splitlines()[2:] if line.strip()]
                  assert font_lines
                  assert all("Noto" in line and line.count("yes") >= 3 for line in font_lines)

                  images = subprocess.check_output(["pdfimages", "-list", str(target)], text=True)
                  image_lines = [
                      line for line in images.splitlines()[2:]
                      if re.match(r"^\s*\d+\s+\d+\s+", line)
                  ]
                  assert len(image_lines) == asset["imageCount"], (
                      asset["name"],
                      len(image_lines),
                  )

                  text = subprocess.check_output(
                      ["pdftotext", "-layout", str(target), "-"],
                      text=True,
                  )
                  assert asset["title"] in text
                  assert asset["author"] in text
                  assert len(marker.findall(text)) == asset["folioCount"], (
                      asset["name"],
                      len(marker.findall(text)),
                  )
                  assert "パブリックドメイン" in text
                  assert re.search(r"CC\s*BY-NC-ND 4\.0", text)
                  assert re.search(r"自筆稿画像を一切転載し\s*ていない", text)
                  assert not any(term in text for term in forbidden)
                  assert "\ufffd" not in text and "\x00" not in text
                  if "tailOrder" in asset:
                      positions = [text.index(value) for value in asset["tailOrder"]]
                      assert positions == sorted(positions), positions
              else:
                  with zipfile.ZipFile(target) as archive:
                      assert archive.testzip() is None
                      names = archive.namelist()
                      assert names[0] == "mimetype"
                      assert archive.getinfo("mimetype").compress_type == zipfile.ZIP_STORED
                      assert archive.read("mimetype") == b"application/epub+zip"
                      container = ET.fromstring(archive.read("META-INF/container.xml"))
                      rootfile = container.find(
                          ".//{urn:oasis:names:tc:opendocument:xmlns:container}rootfile"
                      )
                      package_path = rootfile.attrib["full-path"]
                      package = ET.fromstring(archive.read(package_path))
                      language = package.find(f".//{{{opf_ns}}}metadata/{{http://purl.org/dc/elements/1.1/}}language")
                      assert language is not None and language.text == "ja"
                      cover_items = [
                          item for item in package.findall(f".//{{{opf_ns}}}manifest/{{{opf_ns}}}item")
                          if "cover-image" in item.attrib.get("properties", "").split()
                      ]
                      assert len(cover_items) == 1
                      documents = [
                          ET.fromstring(archive.read(name))
                          for name in names
                          if name.endswith((".xhtml", ".html"))
                      ]
                  text = "\n".join("".join(document.itertext()) for document in documents)
                  image_documents = [
                      document
                      for document in documents
                      if document.findall(f".//{{{xhtml_ns}}}img")
                  ]
                  image_refs = sum(
                      len(document.findall(f".//{{{xhtml_ns}}}img"))
                      for document in documents
                  )
                  japanese = len(re.findall(r"[\u3040-\u30ff\u3400-\u9fff]", text))
                  assert japanese >= asset["minJapanese"], (asset["name"], japanese)
                  assert image_refs == asset["imageRefs"], (asset["name"], image_refs)
                  assert len(image_documents) == 1
                  assert asset["title"] in text
                  assert asset["author"] in text
                  assert len(marker.findall(text)) == asset["folioCount"], (
                      asset["name"],
                      len(marker.findall(text)),
                  )
                  assert "パブリックドメイン" in text
                  assert re.search(r"CC\s*BY-NC-ND 4\.0", text)
                  assert re.search(r"自筆稿画像を一切転載し\s*ていない", text)
                  assert not any(term in text for term in forbidden)
                  assert "\ufffd" not in text and "\x00" not in text
          PY

      - name: Upload release assets and verify downloads
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/current /tmp/verified
          gh release download publications-current --pattern SHA256SUMS.txt --dir /tmp/current --repo "$GITHUB_REPOSITORY"

          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          assets = json.loads(os.environ["ASSET_MANIFEST"])
          names = {asset["name"] for asset in assets}
          current = Path("/tmp/current/SHA256SUMS.txt").read_text(encoding="utf-8").splitlines()
          kept = [
              line for line in current
              if len(line.split(maxsplit=1)) != 2 or line.split(maxsplit=1)[1] not in names
          ]
          kept.extend(f'{asset["sha256"]}  {asset["name"]}' for asset in assets)
          Path("/tmp/SHA256SUMS.txt").write_text("\n".join(kept) + "\n", encoding="utf-8")
          PY

          mapfile -t asset_paths < <(
            python3 - <<'PY'
          import json
          import os
          for asset in json.loads(os.environ["ASSET_MANIFEST"]):
              print("/tmp/ixtlilxochitl-assets/" + asset["name"])
          PY
          )
          gh release upload publications-current "${asset_paths[@]}" /tmp/SHA256SUMS.txt --clobber --repo "$GITHUB_REPOSITORY"

          mapfile -t asset_names < <(
            python3 - <<'PY'
          import json
          import os
          for asset in json.loads(os.environ["ASSET_MANIFEST"]):
              print(asset["name"])
          PY
          )
          for name in "${asset_names[@]}"; do
            gh release download publications-current --pattern "$name" --dir /tmp/verified --repo "$GITHUB_REPOSITORY"
          done
          gh release download publications-current --pattern SHA256SUMS.txt --dir /tmp/verified --repo "$GITHUB_REPOSITORY"

          python3 - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path

          assets = json.loads(os.environ["ASSET_MANIFEST"])
          root = Path("/tmp/verified")
          sums = root.joinpath("SHA256SUMS.txt").read_text(encoding="utf-8").splitlines()
          for asset in assets:
              data = root.joinpath(asset["name"]).read_bytes()
              assert len(data) == asset["size"]
              assert hashlib.sha256(data).hexdigest() == asset["sha256"]
              assert f'{asset["sha256"]}  {asset["name"]}' in sums
          PY

      - name: Remove one-time workflow and staging branch
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          for attempt in 1 2 3; do
            git fetch origin main
            git checkout -B cleanup origin/main
            if [ -f "$WORKFLOW_PATH" ]; then
              git rm "$WORKFLOW_PATH"
            fi
            if git diff --cached --quiet; then
              break
            fi
            git commit -m "Remove completed Ixtlilxochitl release workflow"
            if git push origin HEAD:main; then
              break
            fi
            if [ "$attempt" = 3 ]; then
              exit 1
            fi
          done
          git push origin --delete "$STAGING_BRANCH" || true

      - name: Record workflow failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          failure_path=".github/publication-failures/ixtlilxochitl-five-works-release-$GITHUB_RUN_ID.txt"
          for attempt in 1 2 3; do
            git fetch origin main
            git checkout -B failure origin/main
            mkdir -p "$(dirname "$failure_path")"
            printf 'run_id=%s\nrun_url=https://github.com/%s/actions/runs/%s\n' "$GITHUB_RUN_ID" "$GITHUB_REPOSITORY" "$GITHUB_RUN_ID" > "$failure_path"
            git add "$failure_path"
            git commit -m "Record failed Ixtlilxochitl release workflow"
            if git push origin HEAD:main; then
              exit 0
            fi
          done
          exit 1
