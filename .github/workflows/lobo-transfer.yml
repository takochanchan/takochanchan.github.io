name: Publish approved Lobo Betancur 1667 assets
on:
  push:
    branches: [agent/lobo-betancur-publish-20260919]
permissions:
  contents: write
concurrency:
  group: lobo-betancur-publication
  cancel-in-progress: false
jobs:
  transfer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci --no-audit --no-fund
      - run: npm run build
      - run: npm test
      - name: Upload and re-download approved assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .publication-transfer/transfer-lobo.py
      - uses: actions/upload-artifact@v4
        with:
          name: lobo-release-byte-verification
          path: release-byte-verification.json
