name: Publish Chonay and Saville release assets

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/publish-chonay-saville-release.yml

permissions:
  contents: write

concurrency:
  group: publications-current-release
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Reassemble and verify release assets
        env:
          GH_TOKEN: ${{ github.token }}
          MANIFEST_BLOB: 649d09d43018176cc46cf48a355afdd538f815d8
        run: |
          set -euo pipefail
          manifest=release-manifest.tsv
          gh api "repos/$GITHUB_REPOSITORY/git/blobs/$MANIFEST_BLOB" \
            --jq .content |
            tr -d '\n' |
            base64 --decode > "$manifest"
          test "$(wc -l < "$manifest")" = 5

          while IFS=$'\t' read -r asset_name expected_size expected_sha blob_list; do
            test -n "$asset_name"
            : > "$asset_name"
            for blob_sha in $blob_list; do
              gh api "repos/$GITHUB_REPOSITORY/git/blobs/$blob_sha" \
                --jq .content |
                tr -d '\n' |
                base64 --decode >> "$asset_name"
            done
            echo "$expected_sha  $asset_name" | sha256sum -c -
            test "$(stat -c %s "$asset_name")" = "$expected_size"
          done < "$manifest"

      - name: Confirm the shared checksum file is still current
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir current-release
          gh release download publications-current \
            --pattern SHA256SUMS.txt \
            --dir current-release \
            --repo "$GITHUB_REPOSITORY"
          grep -v -F \
            -e "  Dionisio_Jose_Chonay_Titulo_de_los_senores_de_Totonicapan_1886_Japanese_Complete_Translation.pdf" \
            -e "  Dionisio_Jose_Chonay_Titulo_de_los_senores_de_Totonicapan_1886_Japanese_Complete_Translation.epub" \
            -e "  Marshall_H_Saville_Reports_on_the_Maya_Indians_of_Yucatan_1921_Japanese_Complete_Translation.pdf" \
            -e "  Marshall_H_Saville_Reports_on_the_Maya_Indians_of_Yucatan_1921_Japanese_Complete_Translation.epub" \
            SHA256SUMS.txt > expected-current-SHA256SUMS.txt
          test "$(wc -l < expected-current-SHA256SUMS.txt)" = 136
          cmp expected-current-SHA256SUMS.txt current-release/SHA256SUMS.txt

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release upload publications-current \
            Dionisio_Jose_Chonay_Titulo_de_los_senores_de_Totonicapan_1886_Japanese_Complete_Translation.pdf \
            Dionisio_Jose_Chonay_Titulo_de_los_senores_de_Totonicapan_1886_Japanese_Complete_Translation.epub \
            Marshall_H_Saville_Reports_on_the_Maya_Indians_of_Yucatan_1921_Japanese_Complete_Translation.pdf \
            Marshall_H_Saville_Reports_on_the_Maya_Indians_of_Yucatan_1921_Japanese_Complete_Translation.epub \
            SHA256SUMS.txt \
            --clobber \
            --repo "$GITHUB_REPOSITORY"

      - name: Verify published release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir verified-release
          while IFS=$'\t' read -r asset_name expected_size expected_sha blob_list; do
            gh release download publications-current \
              --pattern "$asset_name" \
              --dir verified-release \
              --repo "$GITHUB_REPOSITORY"
            published="verified-release/$asset_name"
            echo "$expected_sha  $published" | sha256sum -c -
            test "$(stat -c %s "$published")" = "$expected_size"
          done < release-manifest.tsv

      - name: Remove release staging branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api \
            --method DELETE \
            "repos/$GITHUB_REPOSITORY/git/refs/heads/agent/release-assets-chonay-saville-20260731"

      - name: Remove one-time workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          path=.github/workflows/publish-chonay-saville-release.yml
          sha="$(gh api "repos/$GITHUB_REPOSITORY/contents/$path?ref=main" --jq .sha)"
          gh api \
            --method DELETE \
            "repos/$GITHUB_REPOSITORY/contents/$path" \
            -f message="Remove completed Chonay and Saville release workflow" \
            -f sha="$sha" \
            -f branch=main
