name: Verify deployed Rodriguez publication search

on:
  push:
    branches: [agent/rodriguez-publish-20260909]
    paths: ['.github/workflows/rodriguez-verify-remote.yml']

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install verification dependencies
        run: npm ci --no-audit --no-fund
      - name: Wait for the pinned search deployment
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 - <<'PY'
          import json, os, time, urllib.request
          url = "https://api.github.com/repos/takochanchan/takochan-search-index-002/actions/runs/34240846039"
          for attempt in range(90):
              request = urllib.request.Request(url, headers={"Authorization": "Bearer " + os.environ["GH_TOKEN"], "Accept": "application/vnd.github+json"})
              with urllib.request.urlopen(request, timeout=30) as response:
                  run = json.load(response)
              if run["head_sha"] != "efa14f5da5cc7f04c2f9d64d8dc39061a2789d89":
                  raise SystemExit("Unexpected search controller commit")
              if run["status"] == "completed":
                  if run["conclusion"] != "success":
                      raise SystemExit("Search deployment failed: " + str(run["conclusion"]))
                  print("Pinned search deployment succeeded.")
                  break
              time.sleep(10)
          else:
              raise SystemExit("Search deployment did not complete within 15 minutes")
          PY
      - name: Verify exact deployed catalogue and final asset checksums
        run: npm run verify:remote-search
